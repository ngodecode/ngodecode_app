name: {{name.snakeCase()}}_data_impl
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  {{name.snakeCase()}}_data:
    path: ../{{name.snakeCase()}}_data

  {{name.snakeCase()}}_domain:
    path: ../{{name.snakeCase()}}_domain

dev_dependencies:
  flutter_test:
    sdk: flutter

